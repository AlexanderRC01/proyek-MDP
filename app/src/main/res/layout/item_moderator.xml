<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    android:foreground="?attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Icon user (opsional) -->
        <ImageView
            android:id="@+id/moderatorIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_user"
            android:scaleType="centerCrop"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Nama moderator -->
        <TextView
            android:id="@+id/moderatorName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Nama Moderator"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#333333"
            app:layout_constraintStart_toEndOf="@id/moderatorIcon"
            app:layout_constraintTop_toTopOf="@id/moderatorIcon"
            app:layout_constraintEnd_toStartOf="@id/deleteButton"
            android:layout_marginStart="12dp" />

        <!-- Email -->
        <TextView
            android:id="@+id/moderatorEmail"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="moderator@email.com"
            android:textSize="14sp"
            android:textColor="#777777"
            app:layout_constraintStart_toStartOf="@id/moderatorName"
            app:layout_constraintTop_toBottomOf="@id/moderatorName"
            app:layout_constraintEnd_toStartOf="@id/deleteButton" />

        <!-- Optional badge / label -->
        <TextView
            android:id="@+id/moderatorBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Moderator"
            android:textSize="12sp"
            android:padding="4dp"
            android:textColor="#1ABC9C"
            android:background="@drawable/badge_outline_green"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/deleteButton"
            android:layout_marginEnd="8dp"/>

        <!-- Tombol hapus -->
        <ImageButton
            android:id="@+id/deleteButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Hapus Moderator"
            android:src="@drawable/ic_delete"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:tint="#E74C3C" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
