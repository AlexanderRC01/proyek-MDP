<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/scrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".ReportCorrectionActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Judul Halaman -->
        <TextView
            android:id="@+id/titleCorrection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Koreksi Laporan"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:layout_marginBottom="16dp" />

        <!-- Catatan koreksi -->
        <EditText
            android:id="@+id/editTextCorrectionNote"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Masukkan catatan koreksi..."
            android:minLines="4"
            android:gravity="top"
            android:background="@android:drawable/edit_text"
            android:padding="10dp"
            android:textSize="16sp"
            android:textColor="#000000" />

        <!-- Tombol simpan -->
        <Button
            android:id="@+id/btnSaveCorrection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Simpan Koreksi"
            android:textAllCaps="false"
            android:layout_marginTop="24dp"
            android:backgroundTint="#3498DB"
            android:textColor="#FFFFFF" />

    </LinearLayout>
</ScrollView>
